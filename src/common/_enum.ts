export enum MemberRole {
  LEADER = "leader",
  MANAGER = "manager",
  MEMBER = "member",
}

export enum RoomStatus {
  AVAILABLE = "available",
  UNAVAILABLE = "unavailable",
  CONFIRMED = "comfirmed",
  FULL = "full",
}
